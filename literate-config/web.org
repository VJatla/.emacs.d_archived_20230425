* Cold fusion
  #+begin_src emacs-lisp
    (use-package cfml-mode
      :ensure t
      :defer t)
  #+end_src
* web mode
  #+begin_src emacs-lisp
    (use-package web-mode
      :ensure t)
  #+end_src
